{% extends 'Layout/base.html.twig' %}

{% block title %}Register{% endblock %}

{% block navbar %} {% endblock %}


{% block body %}

    <div class="login-form">
        {% for flash_error in app.flashes('verify_email_error') %}
            <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
        {% endfor %}

        <h1>INSCRIPTION</h1>

        {{ form_start(registrationForm) }}
            {{ form_row(registrationForm.nom,{'attr':{'class':'form form-control'}}) }}
            {{ form_row(registrationForm.prenoms,{'attr':{'class':'form form-control'}}) }}
            {{ form_row(registrationForm.email,{'attr':{'class':'form form-control'}}) }}
            {{ form_row(registrationForm.plainPassword, {
                label: 'Mot de passe','attr':{'class':'form form-control'}
            }) }} <br>
            {{ form_row(registrationForm.agreeTerms) }}

            <button type="submit" class="btn btn-primary btn-block">Enregistrer</button>
        {{ form_end(registrationForm) }}
    </div>
{% endblock %}

{% block footer %}{% endblock %}
